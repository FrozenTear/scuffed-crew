# Overview captures

Checked-in software-renderer PNGs were removed — they disagreed with the real Iced window (zero-height Row cards).

Evidence is **real window captures** on niri/Wayland. Drop replacements here when Robert has them.
