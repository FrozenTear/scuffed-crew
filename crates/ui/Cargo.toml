[package]
name = "scuffed-ui"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.8", features = ["csr"] }
uuid = { version = "1", features = ["v4", "js"] }
gloo-timers = { version = "0.3", features = ["futures"] }
scuffed-auth = { path = "../auth" }
