.playwright-mcp/
*.png
!crates/stat-tracker/portraits/*.png
target/
.env
data/secrets.env
# Runtime uploads (host volume / local dev)
data/uploads/
dist/
# Exception: prebuilt-release installer sources (not app SPA output)
!crates/stat-tracker/dist/
!crates/stat-tracker/dist/**
.dioxus/
dist.old/
.worktrees/
*.jpeg
maps/render/
node_modules/
cachyos-repo.tar.xz
.superpowers/
# Local agent / editor state
.serena/
.claude/

# Local agent runtime (memtrace) — never commit
.memdb/
.memtrace/
