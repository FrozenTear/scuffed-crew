.playwright-mcp/
*.png
!crates/stat-tracker/portraits/*.png
target/
.env
data/secrets.env
# Runtime uploads (host volume / local dev)
data/uploads/
dist/
# Exception: prebuilt-release installer sources (not app SPA output)
!crates/stat-tracker/dist/
!crates/stat-tracker/dist/**
.dioxus/
dist.old/
.worktrees/
*.jpeg
maps/render/
node_modules/
cachyos-repo.tar.xz
.superpowers/
# Local agent / editor state
.serena/
.claude/

# Local agent runtime (memtrace) — never commit
.memdb/
.memtrace/

# OCR benchmark ground truth — real scoreboard captures with other players'
# gamertags; local-only, never committed (privacy). Screenshots (*.png) are
# already ignored above; this covers the JSON annotations beside them.
tests/fixtures/replays/ground_truth.json

# X11 capture plan review history — local-only narrative archive; the spec and
# orchestration plan themselves are tracked (docs/superpowers/plans/).
docs/superpowers/plans/2026-07-21-stat-tracker-x11-review-archive.md
